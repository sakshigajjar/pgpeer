# Server
PORT=5000
NODE_ENV=development

# PostgreSQL (Supabase) — paste your connection string from the Supabase dashboard
# Format: postgresql://postgres.[REF]:[PASSWORD]@aws-0-[region].pooler.supabase.com:5432/postgres
DATABASE_URL=

# Auth — JWT signing & cookie behaviour
# Generate JWT_SECRET with:  node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
JWT_SECRET=
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_EXPIRY_DAYS=7
COOKIE_SECURE=false

# express-session secret — used ONLY for OAuth state CSRF protection (not user auth)
# Generate with:  node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
SESSION_SECRET=

# Google OAuth (from Google Cloud Console → Credentials → OAuth Client ID)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback
# Where the OAuth callback redirects the browser after login completes.
# In dev: the React app's URL (Vite dev server).
# In prod: the deployed React URL.
FRONTEND_URL=http://localhost:5173/

# Cloudinary (from cloudinary.com console → Settings → API Keys)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Gemini AI (from https://aistudio.google.com/apikey)
# Used only by GET /api/pgs/:id/summary; cached in DB for 24h to stay within free-tier limits.
GEMINI_API_KEY=

# DEV ONLY — uncomment if cloudinary uploads fail with UNABLE_TO_VERIFY_LEAF_SIGNATURE.
# Disables Node's TLS certificate verification process-wide. NEVER set in production.
# NODE_TLS_REJECT_UNAUTHORIZED=0
